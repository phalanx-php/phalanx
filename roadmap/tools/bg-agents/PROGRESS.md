# bg-agents — Progress Log

[2026-04-28 23:35] Phase 0 — pass — scaffold up, composer install + phpstan level 8 clean, `bin/bg-agents` lists `repl` and `diag`, both placeholder commands print expected output.
[2026-04-28 23:50] Phase 1 — pass — ObservationClient (health/checkpoint/observe/ingest), ObservationQuery, ObservationRecord, BgEvent constants. Diag command live-tested against daemon8 (checkpoint=393387). 8 unit tests green.
[2026-04-29 00:25] Phase 2 — pass — Specialist DTO, SpecialistLoader (md+YAML), Registry, ContextPack/Builder, SpecialistAgent, InvokeSpecialist, AskCommand. Live tested: bg-agents ask supervisor → Anthropic claude-opus-4-7 in 2.5s, 490+72 tokens, observation landed in daemon8 (id 393388, bg.agent.final_answer). 21 unit tests green. DI gotcha logged: ServiceConfigBuilder ->needs(...) creates DFS topo order, factory closure params must be ordered deps-first.
[2026-04-29 00:50] Phase 3 — pass — REPL (line reader, parser, dispatcher, printer, handlers), TeamRunner with heartbeat, team start/status commands, default-route to repl. Live tested: full lifecycle (team.started, heartbeat, agent.final_answer, team.stopped) all landed in daemon8 ids 393398-393401. 34 unit tests green. DI workaround: handlers constructed lazily from $scope->service() to dodge topological-arg-ordering footgun.
[2026-04-29 01:15] Phase 4 — pass — Bookkeeper online: HygieneLane (live duplicate detection over swarm bus), ConsolidationLane (delay-loop, queries /api/observe, clusters, calls gemini-2.0-flash for structured JSON summary), IssueStore (in-memory + write-through to daemon8), BookkeeperRunner. REPL handlers and bookkeeper list/accept/dismiss subcommand. Live tested: 12-record cluster auto-detected and gemini-flash distilled it to 12 distinct facts; dismiss flow round-trips through daemon8. Promotion lane intentionally deferred to Phase 5 (depends on MemoryStore landing). Async footgun logged: Loop::addPeriodicTimer callback fires outside fiber context, so any work that calls scope->await() must use scope->delay() in a while-loop instead. 41 unit tests green.
[2026-04-29 01:35] Phase 5 — pass — MemoryRecord/Query/Store backed by daemon8 observations tagged bg.memory. ContextPackBuilder pulls RAG into specialist context. seed-memory.php helper. memory subcommand + REPL handler. Bookkeeper accept on promotions writes a memory record. Live tested: seeded a resource ownership invariant, queried via 'bg-agents memory player lock', runtime specialist answered with grounded one-liner. Renamed all multi-word commands to single words because Archon's ConsoleRunner only matches top-level verbs (group dispatch wasn't routing the second token).
[2026-04-29 01:50] Phase 6 — pass — SIGINT/SIGTERM handlers in REPL (pcntl when available; degrades gracefully if not), README.md and CLAUDE.md, full morning-demo battery green: composer analyse OK, 41 unit tests pass, diag prints checkpoint+health, ask supervisor returns coherent answer, status reads heartbeat from prior session (cross-session blackboard visibility verified), memory query retrieves seeded RAG record, bookkeeper subcommand reports clean state, help lists 7 commands. End-state: 5 specialists, 3 lanes (hygiene+consolidation live, promotion stub awaiting accept-flow), full RAG round-trip working. Final: composer analyse + composer test clean, no orphaned timers after exit.
