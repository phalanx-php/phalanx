#!/usr/bin/env php
<?php

declare(strict_types=1);

namespace Phalanx\Skopos;

(static function (): never {
    $autoloadCandidates = [
        __DIR__ . '/../vendor/autoload.php',
        __DIR__ . '/../../../vendor/autoload.php',
        __DIR__ . '/../../../../vendor/autoload.php',
    ];

    $autoloaded = false;

    foreach ($autoloadCandidates as $file) {
        if (file_exists($file)) {
            require $file;
            $autoloaded = true;
            break;
        }
    }

    if (!$autoloaded) {
        fwrite(STDERR, "Could not find autoload.php. Run 'composer install' first.\n");
        exit(1);
    }

    $configFile = getcwd() . '/skopos.php';

    if (!file_exists($configFile)) {
        fwrite(STDERR, "No skopos.php found in " . getcwd() . "\n");
        fwrite(STDERR, "Create a skopos.php that returns a DevServer instance.\n");
        exit(1);
    }

    $result = require $configFile;

    if (!$result instanceof DevServer) {
        fwrite(STDERR, "skopos.php must return an instance of " . DevServer::class . "\n");
        exit(1);
    }

    exit($result->run());
})();
