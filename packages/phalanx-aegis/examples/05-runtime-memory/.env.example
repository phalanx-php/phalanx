# No environment variables required for this demo.
# RuntimeMemoryConfig row sizes are injected via AppContext::with() inside the demo closure.
# Symfony Runtime populates $context from the process environment and .env files.
